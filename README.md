
#### 1) What is the difference between `null` and `undefined`?
Null : user defined a value as nothing intentionally.
Undefined : user not defined a value. so run time browser or node defined a value it's undefined.


#### 2) What is the use of the `map()` function in JavaScript? How is it different from `forEach()`?
Map : map return us. return - obj, index and array. 
ForEach : it's not return anything 

#### 3) What is the difference between `==` and `===`?
== (double equal) just check value not type . but === (triple equal) also check value and type. 
best practice to use ===.
#### 4) What is the significance of `async`/`await` in fetching API data?
asynchronous code that looks and behaves like synchronous code

#### 5) Explain the concept of Scope in JavaScript (Global, Function, Block).
Global scope : value access from any where just except dead zone. (just use before initialization)
Function scope : we define a variable into function, it's function scope.
Block scope : block means {} under carly bracket.